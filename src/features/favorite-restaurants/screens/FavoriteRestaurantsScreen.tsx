import { SafeAreaView, Text } from "react-native";

const FavoriteRestaurantsScreen = () => {
    return (
        <SafeAreaView>
            <Text>Favorite Restaurants Screen</Text>
        </SafeAreaView>
    );
}

export default FavoriteRestaurantsScreen;